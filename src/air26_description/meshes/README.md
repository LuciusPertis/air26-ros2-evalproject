# meshes/

The base rover is built entirely from URDF primitives (boxes/cylinders), so there
are no mesh files to ship. If your Task 1 addition needs a mesh, drop the `.stl` /
`.dae` here and reference it as `package://air26_description/meshes/yourfile.stl`.
